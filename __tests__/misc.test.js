describe('Test Subtract', function () {
    it('Should be truth', function () {
        expect(5 - 9).toEqual(-4);
    });

    it('Should be truth', function () {
        expect(2 + 3).toEqual(5);
    });
});
